sp {sub-column*propose*get-sub2
   (state <s> ^name sub-column
             -^sub2)
-->
   (<s> ^operator <op> + =)
   (<op> ^name get-sub2)
}

sp {sub-column*apply*get-sub2
   (state <s> ^operator.name get-sub2
              ^current-column.sub2 <a>)
-->
   (<s> ^sub2 <a>)
}


