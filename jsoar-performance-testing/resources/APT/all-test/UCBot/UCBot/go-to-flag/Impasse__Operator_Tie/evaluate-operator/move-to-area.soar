sp {evaluate-operator*apply*move-to-area
   (state <s> ^superstate.operator.name evaluate-operator
              ^top-state <ts>
              ^operator <op>)
   (<ts> ^map <map>)
   (<op> ^name move-to-area
         ^path.next-area <to-area>)
   (<map> ^current-area <current-area>)
-->
   (<map> ^current-area <current-area> -
                        <to-area>)
}