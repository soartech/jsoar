sp {msg*elaborate*state
   (state <s> ^name msg
              ^superstate.operator.message <m>)
-->
   (<s> ^supported all label confirmed goto avoid allow circuit-patrol linear-patrol initiate terminate heuristic diffuse-wire
        ^message <m>)
}
