sp {msg*elaborate*top-state*label
   (state <s> ^superstate nil)
-->
   (<s> ^labels <l>)
}

sp {elaborations*elaborate*unique-labels
   (state <s> ^labels <labels>)
   (<labels> ^label.name <l>)
-->
   (<labels> ^unique <l>)
}
