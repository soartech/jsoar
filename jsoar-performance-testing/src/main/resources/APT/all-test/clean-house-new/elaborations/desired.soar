## JEL 10-15-10 added go-to-area because it generates its own desired
sp {elaborations*elaborate*desired
   (state <s> ^superstate.desired <d>
              ^name << deposit-object go-to-area goto-storage-area >>
              ^impasse no-change
              ^attribute operator)
-->
   (<s> ^desired <d>)
}

#sp {elaborations*elaborate*problem-space
#   (state <s> ^superstate.problem-space <ps>
#              ^impasse no-change
#              ^attribute operator)
#-->
#   (<s> ^problem-space <ps>)
#}

sp {elaborations*elaborate*problem-space*eval
   (state <s> ^superstate.superstate.problem-space <ps>
              ^impasse no-change
              ^attribute operator
              ^name evaluate-operator)
-->
   (<s> ^problem-space <ps>)
}
