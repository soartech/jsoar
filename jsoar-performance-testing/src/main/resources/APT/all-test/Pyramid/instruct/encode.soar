sp {instruct*propose*encode
   (state <s> ^name instruct
              ^current-problem <p>)
   (<p> -^encoded true
         ^type solve
         ^initialized true)
-->
   (<s> ^operator <op> + =)
   (<op> ^name encode)
}
