sp {compute-alternative-dice-counts*elaborate*superstate*failed
   (state <s> ^name compute-alternative-dice-counts
              ^last-bid failure
              ^superstate <ss>)
-->
   (<ss> ^alternative-dice-counts failure)
}

