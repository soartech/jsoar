

# carry out the specified secondary action
sp {move-and-x*propose*step-2-x
   (state <s> ^type move-and-x
              ^at-target yes
              ^next-action <na>)
   (<na> ^name <name>
         ^type <type>)
 -->
   (<s> ^operator <o> + >)
   (<o> ^name <name>
        ^type <type>)
}