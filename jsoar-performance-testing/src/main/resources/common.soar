# Put any common production rules / configuration settings here.

# Not needed because none of the tests use log.  In addition this
# will cause CSoar tests to fail miserably because handler
# doesn't exist in CSoar.

#handler --disable log
